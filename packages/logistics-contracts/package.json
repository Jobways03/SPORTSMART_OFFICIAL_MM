{
  "name": "@sportsmart/logistics-contracts",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript + Zod schemas for the logistics facade. Consumed by apps/logistics-facade for runtime validation and by apps/api for type-only imports.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'no lint configured yet'"
  },
  "peerDependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "zod": "^3.23.8"
  }
}
