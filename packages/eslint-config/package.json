{
  "name": "@sportsmart/eslint-config",
  "version": "0.0.1",
  "private": true,
  "description": "Shared ESLint configuration for SportsMart frontend apps. Frontends extend nextjs.json (legacy .eslintrc shape) today; if/when ESLint flat-config becomes mandatory across the toolchain, add a nextjs.mjs sibling.",
  "main": "nextjs.json",
  "dependencies": {
    "eslint-config-next": "^15.5.14"
  },
  "peerDependencies": {
    "eslint": ">=8"
  }
}
