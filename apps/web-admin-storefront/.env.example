NEXT_PUBLIC_API_URL=http://localhost:8000

# Feature flags — parked features (post-MVP1)
# Set to "true" to enable the sidebar entry and route. When "false" (default),
# the route returns notFound() and the sidebar entry is hidden.
NEXT_PUBLIC_FEATURE_NOVA=false
NEXT_PUBLIC_FEATURE_REPLACEMENTS=false
